# Shopify POS loyalty — local release review packet

Worktree: `C:/Users/jeffr/Degen-consolidation/shopify-pos-loyalty-20260910` (WSL: `/mnt/c/Users/jeffr/Degen-consolidation/shopify-pos-loyalty-20260910`). Base: `cbc7896fd054881ce1620b3e5ffa7051040da535`. All feature work is uncommitted. The original feature/source WIP is preserved; the authorized additive candidate worktree and its latest verification are recorded below. Other existing worktrees were not changed.

Status (September 11, 2026): **current-main candidate and required full suite verified locally on Python 3.11/PostgreSQL 17; production launch is blocked, with source defaults OFF and no activation approved.** Jeffrey approved all cashiers viewing balances/history, with corrections restricted to authorized Ops controls. No extra employee account, Ops mapping or loyalty PIN/login is required. This policy is settled; production deployment and activation are not approved. The native DEV screenshot confirms 19 points, −1 refund/+20 purchase history and header Refresh. Native phone offline/reconnect/sleep/lock behavior remains unverified; automated simulations are distinguished below.

The preceding automated review changed only one cashier-facing message (review cases now direct staff to an Ops administrator), focused extension tests and these readiness/walkthrough documents. Existing feature work and other worktrees were preserved. No backend accounting/authentication semantics, source flags, native process, tunnel or handoff inputs were changed. That pass made no production access, Shopify write, release, commit or push. The subsequent read-only readiness attempt is recorded below.

## Current-main release candidate — Python 3.11 / PostgreSQL 17 verification

**Candidate:** `/home/jeffr/degen-candidates/loyalty-green-20260911`, branch **codex/loyalty-green-candidate-20260911**, base **f40bb83d2b5585a029cbfb0808e907bd7267ffdc**. `git ls-remote origin refs/heads/main` verified that exact live origin/main SHA before preparation. A new worktree/branch was added from the already available object; no fetch, merge, rebase, reset or cleanup touched the dirty feature branch. **The candidate remains uncommitted; its base SHA is not a feature release SHA and deployment is not authorized.** This section supersedes the historical local runtime/full-suite gaps below for this candidate only.

Preparation metadata and review manifests: `/tmp/loyalty-candidate-preparation.json`, `/tmp/loyalty-candidate-source-manifest.json`, `/tmp/loyalty-candidate-content-manifest.json` (the content manifest excludes this mutable release packet).

Copied scope: **7** reviewed shared-file patch targets and **77** approved new loyalty/backend/router/template/test/document/extension/dev-tooling files. Ignored dependencies, generated bundles, credentials, handoff inputs and app databases were not copied. The original feature hashes were preserved; shared file content also matches after CRLF normalization. Protected inventory ingestion, TikTok signing and shared Admin API files match candidate main. All five runtime source flags remain OFF; production identifiers/secrets/activation values remain unset in code. Reviewed dev-app configuration stays development-only.

Candidate-only fixes are limited to test infrastructure:

- `tests/test_employee_hours_consistency.py`: the empty-cache date-boundary fixture now mocks Clockify's empty response and asserts the expected UTC request window. The original failure reproduced on clean current main with the same isolated Python 3.11 harness; no payroll/Clockify production code changed.
- `scripts/loyalty_synthetic.py`: standard asyncio is selected in the synthetic process, preventing optional uvloop's native sockets from bypassing Python listener-ownership tracking. Clean current main reproduced the denied-loopback browser failure with the previous harness. The network guard still blocks external access.
- `tests/test_loyalty_visual.py`: removed the machine-specific Windows Chromium path and registered its fulfill-or-abort fixture after the page guard, allowing synthetic HTML/local assets without network access.
- `extensions/loyalty-pos/playwright.config.ts` and `tests/browser/preview.spec.ts`: accept a selected preview URL and use that same origin for browser request isolation. Candidate browser testing used **127.0.0.1:18767**, preserving existing previews 8766/8767 and native processes. This temporary candidate server was stopped after testing.

**Isolated runtime:** Python **3.11.15**, PostgreSQL server **17.9**, FastAPI **0.136.1**, SQLModel **0.0.38**, SQLAlchemy **2.0.49**, psycopg **3.3.3**, candidate PyJWT **2.13.0**. The existing Python interpreter created a new `/tmp/loyalty-green-python311` venv. Official PGDG Ubuntu 24.04 PostgreSQL 17.9 packages were SHA256-checked against the HTTPS package index and extracted under `/tmp/loyalty-green-pg17-binaries`; no system package/service was installed. This matches Green's Python/PostgreSQL versions and selected core dependencies, not its complete OS/dependency image. Full isolated package versions: `/tmp/loyalty-candidate-dependencies.json`.

Each PostgreSQL run initialized a fresh `loyalty-worker-pg-*` scratch cluster, with TCP listening disabled, port 55439 on its own Unix socket, synthetic `loyalty_test` database and teardown in `finally`. No existing local or production database was used. Tests run with synthetic home/env, dotenv disabled, external providers/workers disabled and external network blocked. Browser binaries use an explicitly selected disposable cache.

| Final candidate verification | Exact result / evidence |
|---|---|
| Required full repository suite, Python 3.11.15 + disposable PostgreSQL 17.9 | **4466 passed, 7 skipped, 1 warning, 239 subtests passed in 1135.05s (0:18:55)**; `/tmp/loyalty-candidate-full.log`, JUnit `/tmp/loyalty-candidate-full.xml`. Completed; not a timed partial pass. |
| Loyalty cases within that same final full run | **262 passed, 1 skipped**, counted from JUnit. Includes SQLite/PostgreSQL domain, ledger concurrency/fencing, catalog corruption/readiness, endpoint/auth, lifecycle/reconciliation and synthetic Ops/native fixtures. SQLite skips the PostgreSQL-only numeric corruption variant; the PostgreSQL variant is exercised. |
| Clockify fixture regression module | **10 passed**, 2.35s; `/tmp/loyalty-candidate-clockify-fixed.log`. Before-fix candidate and clean-main reproductions: `/tmp/loyalty-candidate-clockify-before.log`, `/tmp/loyalty-candidate-clockify-baseline.log`. |
| Chromium/WebKit and loyalty visual/demo fixture check after isolation repair | **15 passed**, 24.58s; `/tmp/loyalty-candidate-browser-fixtures-fixed.log`. Previous harness failure reproduced on clean main: `/tmp/loyalty-candidate-loop-baseline.log`. |
| Extension component / real loopback browser tests | **32 component tests passed**; `/tmp/loyalty-candidate-components.log`. **2 browser tests passed**, 3.3s; `/tmp/loyalty-candidate-pos-browser.log`. Synthetic customer switching, offline clearing/reconnect, mobile layout/history and origin/host rejection passed. Mobile screenshot opened and inspected; this does not establish native phone lifecycle behavior. |
| Typecheck / build / compile / dependencies / whitespace | Passed. `/tmp/loyalty-candidate-typecheck.log`, `/tmp/loyalty-candidate-build.log`, `/tmp/loyalty-candidate-compile.log`; `pip --isolated check` reports no broken requirements. |

The final full-suite skips comprise **six root/Windows-specific backup checks and one SQLite instance of a PostgreSQL-only numeric corruption check**. The PostgreSQL corruption variant passes. These are reported skips, not verified root/Windows execution. The single warning is the existing `discord` dependency’s `audioop` deprecation under Python 3.11.

The preliminary focused invocation recorded **261 passed, 1 failed, 1 skipped** before the offline visual fixture repair; it is superseded by the successful fixture checks and final full-suite result, not hidden. Its log is `/tmp/loyalty-candidate-focused.log`. Initial browser-isolation failures are retained in `/tmp/loyalty-candidate-browser-fixtures.log`. Historical full-suite failures below remain historical; the candidate's narrow fixture repairs were not backported into the preserved original source worktree.

Exact verification entrypoints, run from the candidate:

```bash
# Fresh PostgreSQL bootstrap, bounded full suite, automatic scratch-cluster stop:
python3 /tmp/loyalty-candidate-full-check.py > /tmp/loyalty-candidate-full.log 2>&1
# This saved runner invokes scripts/run_loyalty_tests.py with tests, --loyalty-ui,
# --loyalty-pg-socket <new scratch socket>, --loyalty-max-seconds 7200,
# explicit Chromium/cache, --tb=short, -q, -rs, --durations=15 and JUnit output.
# Exact expanded argv from the completed run:
cat /tmp/loyalty-candidate-full-command.json
python3 scripts/run_loyalty_pos_node.py npm run typecheck
python3 scripts/run_loyalty_pos_node.py npm run build
python3 scripts/run_loyalty_pos_node.py npm test
/tmp/loyalty-green-python311/bin/python -m compileall -q app scripts tests
/tmp/loyalty-green-python311/bin/python -m pip --isolated check
```

The synthetic POS preview can be relaunched locally with `/tmp/loyalty-green-python311/bin/python scripts/loyalty_pos_demo.py --port 18767`. Browser invocation used `scripts/run_loyalty_pos_node.py node --input-type=module -e 'import {spawnSync} from "node:child_process"; process.env.LOYALTY_PREVIEW_URL="http://127.0.0.1:18767"; const r=spawnSync(process.execPath,["node_modules/@playwright/test/cli.js","test"],{stdio:"inherit",env:process.env}); process.exit(r.status??1)'`. No Shopify CLI login/tunnel/native process was involved.

**Readiness verdict:** this current-main candidate passes local runtime/full-suite verification; the focused final source review and synthetic restore rehearsal below are complete. A reviewed feature commit/release SHA, verification against any subsequently changed main, backup checksum/off-host/restore evidence and separate exact deployment approval are still required. Missing `read_returns`, minimum field/protected-data access, app-configured webhook inventory/signing setup, named owner, spending budget, retention and activation cutoff remain distinct integration/launch gates. Approved sole location **83278725336** and the all-cashier read/Ops-correction policy are unchanged.

**Rollback/preservation:** nothing was deployed or activated. Original feature code/WIP and existing previews remain intact. Rejecting a candidate-only fixture change requires revised candidate verification; no production rollback is necessary for this local stage. The existing additive-schema/data-preserving deployment rollback below still applies only after a separately authorized future deployment. No commit, push, merge-to-main, production read/write, subscription or credential change occurred in this candidate stage.

## Final candidate review and backup rehearsal — September 11

**Outcome:** no new release-blocking accounting, identity, authentication, worker or schema defect was found in this bounded source/diff review. No implementation change was needed. This is a local candidate review, not a claim of verified production accounting, native phone lifecycle or disaster recovery. The previous **4466 passed / 7 skipped / 239 subtests**, **262 loyalty passed / 1 skipped**, **32 component / 2 browser passed**, typecheck/build and compile evidence remains applicable; unchanged source did not warrant another full-suite run.

| Review area | Checked outcome and practical boundary |
|---|---|
| Accounting and refunds | Exact integer cents, cumulative half-up whole-point entitlement, immutable original discount/tax basis and refund-difference postings. Ordinary completed returns with clear successful line refunds work; ambiguous allocations/exchanges/edits review. Failed payment attempts and supported cash rounding do not invent credit. POS/location/created-and-processed cutoff checks remain prospective; online is excluded at launch and TikTok permanently. |
| Identity and administrative controls | Accounts remain shop/customer-ID bound. Canonical local observation or audited verified evidence establishes attachment timing; unsigned event headers cannot backdate it. Account reassignment, changed program policy/basis and successful merges hold for review; failed merges do not freeze accounts. Ops correction/replay use the same order lock/lease and current permissions/CSRF, with audited compensating entries. Successful-merge release remains an operational gate, not a delivered button. |
| Durable processing | Delivery uniqueness and business-posting uniqueness are distinct. First inserts use conflict-safe database operations; order/account locks, revision/lease fencing and bounded transaction retries protect postings. Stale canonical data cannot reverse newer results. Overlapping reconciliation detects repeated cursors and recovers known pending/awarded orders separately. The shop program lock serializes final posting transactions; production capacity still needs measurement. |
| POS read isolation | Verified HS256 bearer audience/issuer/destination/session claims, exact configured shop/app binding, no cookie fallback, bounded requests and signed customer-bound history cursors. Balance/history share one SQL snapshot; errors do not become zero balances. Customer/session/connectivity changes invalidate pending responses and visible data. All-cashier reads grant no Ops corrections and claim no verified PIN identity. Rate limits remain process-local. |
| Schema and shared integration | Five additive tables, BIGINT point/money capacity, unique/check/FK/index readiness and append-only trigger were reviewed with existing PostgreSQL corruption/concurrency tests. Models register before schema initialization. All five feature flags default OFF; startup still creates tables/function/trigger and missing permission defaults. Existing partial/malformed tables require catalog review and fail closed rather than destructive repair. Inventory ingestion, TikTok signatures and shared Admin version remain unchanged. |

**Exact candidate scope:** `/home/jeffr/degen-candidates/loyalty-green-20260911`, `codex/loyalty-green-candidate-20260911`, base `f40bb83d2b5585a029cbfb0808e907bd7267ffdc`. The **84-entry** filename/content-hash manifest is `/tmp/loyalty-candidate-content-manifest.json`, SHA256 **2b0d3d7ae2e85caafe3a6e27d460bbad62ea38e20697d7f991f587c6459b15ca**. It covers every candidate file except this mutable packet: **85 changed/new files total**, including the narrow Clockify test fixture repair. Every manifest entry was rechecked unchanged after this review. Preserve that manifest and the test logs with the eventual reviewed release record; `/tmp` is not durable release storage. No release commit exists: **RELEASE_SHA is UNSET**. The original feature worktree and existing previews were not modified in this review.

### Existing Green backup evidence — read-only metadata

At **2026-09-11 21:25:15 UTC**, authenticated Brev metadata identified **openclaw-9902ae / vmc5a2549 / RUNNING**. The existing managed connection reached guarded host **brev-vmc5a2549**. Inspection read bounded service properties, artifact stats, allowlisted operation-state fields and four small installed program hashes only. No app imports, database queries, dump contents, secret/config values, backup helper execution, new backup, copy/export, restore or production mutation occurred. Evidence: `/tmp/loyalty-backup-review-metadata.sh` and `/tmp/loyalty-backup-review-metadata.log`.

- Backup timer **active/waiting**; latest run **10:23:12–11:37:11 UTC September 11**, service result **success**, exit **0**; next trigger **September 12 10:25:57 UTC**. Completed oneshot service is normally inactive/dead.
- Two local dump artifacts and both checksum sidecars exist. Latest dump: **21,177,519,272 bytes**, mtime **September 11 11:06:57 UTC**, about **10h18m** before inspection. Previous: **21,162,575,093 bytes**, mtime **September 10 11:05:36 UTC**. Sidecar existence does **not** verify checksum validity. No 21 GB checksum scan was performed.
- Bounded inspection found **seven** timestamped operation-state records (July 6–10). The newest, **20260710T042901Z**, is `source_verified` against commit `bde0441c70f562b49045bf2ab7b3c749b0ccdb5f`; the preceding **20260709T060538Z** is `policy_enabled`. None of the inspected records contains a populated observation run/digest receipt or a top-level restore field. These historical states do not establish a current backup failure, and the bounded inspection cannot prove that no other recovery evidence exists.
- Installed backup runner, environment parser and retention helper hashes match the candidate's reviewed backup asset manifest. **Installed operations helper differs:** observed SHA256 `9d1dacdab6015a4b1f2a5a74c7c6169d2afac7f4fd7abb0a084de2c409c8a8c0`, candidate manifest expects `e730076be46d9c6c9095e5f1397bcf056bd3776ba8b1563511ba016cf26269b6`. Do not run the candidate's observation/recovery command as though installed helper provenance matched. The follow-up below resolves the installed version to a recorded older installation; no helper replacement is required by its standalone restore plan or included in loyalty deployment.

The [backup runbook](../green-postgres-backup-runbook.md) explicitly records a canceled full restore rehearsal and distinguishes archive listing from restore proof. **No completed actual-production-data restore proof was established in this review.** A fresh valid checksum, verified off-host copy and completed production-backup recovery evidence remain gates. Scheduled success and matching runner code alone do not close them. Any actual-data restore rehearsal needs separate authorization for the exact artifact, isolated target, access controls and disposal; production data was neither exported nor restored here.

### Synthetic PostgreSQL 17 dump/restore — passed

Reproducible local command: `python3 /tmp/loyalty-synthetic-restore-check.py > /tmp/loyalty-synthetic-restore.log 2>&1`. Result: `/tmp/loyalty-synthetic-restore-result.json`. The script creates its own socket-only PostgreSQL **17.9** cluster, uses isolated Python **3.11.15** synthetic home/env with dotenv/external networking/providers disabled, and stops only that cluster in `finally`. No existing database or preview is used. Scratch path: `/tmp/loyalty-worker-pg-eignzp9i` (server stopped after success).

The rehearsal used PostgreSQL 17 `pg_dump --format=custom`, verified the synthetic SHA256 sidecar, then ran `pg_restore --exit-on-error --single-transaction --no-owner --no-acl` into a separate fresh scratch database. The **27,431-byte synthetic archive** has SHA256 `ba934bab4b13432d7c84e3937ec5782127fb3ed31a4d8fc9b830f371f7445049`.

- All five table fingerprints matched before/after restore: **1 account, 2 entitlements, 2 ledger rows, 3 inbox rows, 1 reconciliation row**, including a pending worker lease and durable reconciliation cursor.
- **Schema readiness passed before any post-restore installer ran**, including types, constraints, indexes and immutable trigger. Balance **19** and ledger **+20, −1** were preserved; ledger UPDATE and DELETE were rejected.
- Repeated delivery stayed deduplicated. The restored expired lease was reclaimed and its old worker token rejected. A pending synthetic $1 order posted **+1** through restored sequences; redelivery of the refunded order posted **0**. Final balance **20**, three ledger entries, and ledger rebuild reconciliation had no discrepancies.

This validates the narrow synthetic loyalty schema/data recovery path and post-restore behavior. It does **not** validate the 21 GB production archive, other application tables, production privileges/extensions, off-host storage, recovery duration/capacity, production RPO/RTO or the installed backup orchestration helper. Benign client warnings about the deliberately inert `/dev/null` password-file setting are retained in the log; connections used only the new trust-authenticated Unix socket.

**Next minimal release step:** retain the reviewed candidate and obtain the missing production recovery evidence under the explicit data-handling plan below. The follow-up resolves the installed helper mismatch to a known older installation; it does not mandate an upgrade. Separately authorize producing the reviewed candidate commit to identify a real release SHA; no commit/push is authorized by this packet. Recheck then-current main and require the full suite on any changed final candidate. Only then request approval for the exact **disabled** normal-deploy SHA, five-table/permission effects, dependency installation, configuration synchronization and web/worker/user-Ops-bot restart scope listed below. A main push deploys automatically, so publishing is part of that later approval. Keep **all five runtime flags false**; Shopify configuration/scopes/subscriptions/native release and accounting/POS activation remain separate. Owner, budget, retention/disposal and UTC cutoff are still unanswered launch inputs; the sole location and cashier policy already have approval.

## Backup-helper provenance and proposed actual-data restore — September 11 follow-up

**Provenance resolved; no helper upgrade is a prerequisite for the standalone restore plan.** At **2026-09-11 21:46:48 UTC**, authenticated Brev identified **openclaw-9902ae / vmc5a2549 / RUNNING**, and the managed connection reached **brev-vmc5a2549**. The installed helper is a regular **892,615-byte, root:root, 0755** file, mtime **July 9 06:29:59 UTC**. Its SHA256 **9d1dacdab6015a4b1f2a5a74c7c6169d2afac7f4fd7abb0a084de2c409c8a8c0** matches the complete repository helper blob at **de18041d0bed1d673e7b73698e5947c5af7dd4b9** (`fix: accept canonical backup receipt key order`). Local history compared 22 helper-changing commits / 17 distinct helper hashes; the same historical blob match was independently verified on Green. No unknown helper was executed or copied to run locally.

| Provenance evidence | Finding |
|---|---|
| Completed installation receipt | `/opt/degen/backups/config/20260709T060538Z/operation-state.json` records the installed hash in **reviewed_source.asset_hashes**, **host_stage.asset_hashes** and **install.installed_hashes**. Its retained source file has the same hash. Installation validated **July 9 06:30:04 UTC**, completed **06:30:05 UTC**; prior inspection shows `policy_enabled`. Its reviewed commit **b0c81e7a6bde9b9e6d2901a21bba06360438a430** is the merge of the de18041 fix. This establishes normal older installed provenance, not arbitrary source drift. |
| Later reviewed source | `/opt/degen/backups/config/20260710T042901Z` records reviewed **bde0441c70f562b49045bf2ab7b3c749b0ccdb5f**, source hash **ec2c05f9db792ea5705c05364de054f911b909fbd58ebffb3e9c37da1d8a3559**, matching its retained source. It remains `source_verified`, with no staged/install helper hash or completed-install receipt. This evidence explains why the later reviewed source did not replace the installed helper; it does not establish who chose to stop or why. |
| Current checkout versus installed executable | Both deployed checkout file and HEAD blob have candidate-expected **e730076be46d9c6c9095e5f1397bcf056bd3776ba8b1563511ba016cf26269b6**. Repository updates did not themselves reinstall `/usr/local/sbin/degen-prod-db-backup-ops`. Later helper changes are observation-before-quiescence (**bde0441**) and enabled-policy upgrade/live-snapshot validation (**c547509**, **956194d**, **2e48874**). |
| Practical limitation of older helper | The newer observation fix captures stable service/timer invocation metadata before quiescence; the older observation path can lose that evidence when systemd clears inactive-unit timestamps. Later upgrade paths also have additional policy/staging checks. Do not use current-runbook observation/upgrade commands against the older executable. This is an operations/verification limitation, not evidence of corrupt dumps or failed scheduled backups. |
| Scheduled backup independence | Live `degen-prod-db-backup.service` ExecStart uses **`/usr/local/sbin/degen-prod-db-backup`**, not the operations helper. Its latest run still reports **success / exit 0**, September 11 **10:23:12–11:37:11 UTC**. The separately hash-verified runner's source performs archive-list validation, computes a checksum, publishes the local pair and performs remote publishing; that explains the intended success path but is not fresh independent archive/off-host verification. No timer, helper or policy change is needed for the direct PostgreSQL plan below. |

**New release-base fact:** Green HEAD is now **83f18502fd27f89272e35f0e12c35cc6d3986133** (`Improve listing pricing, mobile details, and full-logo references (#40)`), replacing the previously observed f40bb83 production baseline. Candidate HEAD/base remains **f40bb83d2b5585a029cbfb0808e907bd7267ffdc**; its tested contents were preserved. This makes a new current-main comparison and any resulting candidate integration/verification a release gate. **The old f40bb83 value is not the current rollback target.** Reverify actual Green HEAD at the eventual deployment window; no candidate merge/rebase or production fetch/pull was performed here.

Evidence: `/tmp/loyalty-backup-provenance-local.json`, `/tmp/loyalty-backup-provenance.sh`, `/tmp/loyalty-backup-provenance.log`, `/tmp/loyalty-backup-unit-check.sh`, `/tmp/loyalty-backup-unit-check.log`. Remote reads were limited to known source blobs, small allowlisted asset/receipt metadata, artifact stats/sidecar and host capacity. No dump contents, customer/order rows, environment values, secrets or application initialization were read. Only this candidate packet changed; the 84-file content manifest remains unchanged and no tests were rerun.

### Pinned artifact and capacity — facts, not restore authorization

- **Source:** `/opt/degen/backups/db/degen_green_prod_brev-vmc5a2549_20260911T102316Z.dump` on the verified Green host. **21,177,519,272 bytes** (19.72 GiB), mtime **2026-09-11T11:06:57.596887Z**, root:root **0600**, observed device **2049**, inode **5453724**.
- **Sidecar:** same path plus `.sha256`, mtime **2026-09-11T11:08:33.570807Z**. Bounded read verified its strict digest/filename format and exact basename binding. Recorded SHA256: **85d14e4fcc7837990e3d548c9b88bec24462f771f62b74c19fa7acef5e6d3487**. **This is the recorded expected checksum, not a fresh checksum of the 21 GB archive.** No expensive archive scan was performed. Never silently substitute a newer or changed artifact; re-pin it if retention removes this one.
- **Green:** 4 CPUs, about **15.62 GiB RAM / 8.24 GiB available**, no swap; backup filesystem **58.22 GiB available**. Restored size/WAL/index workspace is unknown. A restore on Green is not proposed: it would contend with production and has insufficient demonstrated headroom.
- **Proposed target:** this machine's **Ubuntu-24.04 WSL**, task directory `/home/jeffr/degen-restore-rehearsals/green-20260911`, separate from candidate/source worktrees and all previews. WSL reports **923.4 GiB available**, 16 CPUs, about **30.19 GiB RAM / 28.50 GiB available**. Read-only distribution metadata confirms its backing drive is **C:**; C: reports **2,914,417,233,920 bytes available** (about 2.65 TiB). These are point-in-time capacity facts, not guaranteed uncompressed restore size.
- **Unmet local isolation gate:** C: encryption/protection status could not be obtained with available read-only metadata access. Verify protected storage, swap/pagefile/hibernation handling and any automatic backup/sync coverage before copying confidential data. `unshare` and `systemd-run` exist; the subsequent synthetic empty-target preflight below now verifies namespace, filesystem and per-task cgroup controls. Storage encryption evidence remains blocked. Docker, Podman and bwrap are absent. Do not install tooling, assume isolation works or start a restore until an empty-target preflight establishes the proposed controls. No paid capacity/subscription or budget increase is proposed or approved.

### Concrete proposed rehearsal — execute only after separate approval

The recommendation is **one full logical restore of that exact archive into a new, isolated local PostgreSQL 17.9 cluster**, with no application startup, loyalty schema installation, production database connection or real-record browsing. This tests recoverability of the existing application backup; because production predates loyalty tables, the separate synthetic rehearsal remains the loyalty-specific recovery evidence.

1. **Empty-target safety preflight.** Fail if the proposed task directory already exists; never reuse or remove unrelated paths. Verify protected storage and absence of unintended cloud sync/backup capture. Prepare owner-only directories/files (**0700/0600**, umask 077), a credential-free home and a confined filesystem exposing only the restore scratch space, required PostgreSQL binaries/libraries read-only, and a read-only archive mount. Run PostgreSQL/restore without OS root in a private network namespace with **no egress and no TCP listener**, a private Unix socket under `<task>/socket`, port **55447**, database **loyalty_restore_20260911**, and a scratch-only role. No inherited env/auth stores or host home mounts. Verify isolation using synthetic probes before permitting the confidential copy. If namespace/filesystem/resource controls cannot be established with reviewed existing tools, stop with no production data copied; do not fall back to an ordinary networked process.
2. **Proposed resource/time bounds.** Use existing PostgreSQL 17.9 binaries already verified in the candidate stage. Limit the local restore group to **2 CPU equivalents / 8 GiB RAM / no swap** and one restore job; keep **200 GiB** as the total scratch planning limit, abort at **180 GiB measured usage** or less than **100 GiB free** on either WSL/backing storage. Enforce a scratch quota where supported; otherwise explicitly verify the watchdog/abort behavior and retain headroom rather than claiming a hard filesystem quota. Check usage at most every five seconds. Proposed transfer limit **8 MiB/s / 90 minutes**; restore limit **6 hours**. These are proposed ceilings, not measured completion estimates or an approved spending budget; stop rather than raise limits automatically. Do not change global PostgreSQL/WSL settings or existing services.
3. **One controlled copy, then checksum.** Reverify authenticated host and the exact root-owned regular-file artifact/sidecar identity and unchanged metadata. Use the existing managed SSH transport, a no-follow read-only source descriptor, and low-priority bounded reads; no new Green file, permission, credential, backup lock or timer change. Stream binary output straight into a new owner-only local `.partial` file through subprocess capture, never a terminal/tool transcript or a Brev wrapper that adds text. Hash while receiving, require exact byte count and the pinned SHA256, and recheck source descriptor metadata before/after the transfer. Publish only the private local completed file after a match. Avoid a second 21 GB source hash scan. If deletion, metadata change, checksum mismatch, timeout or production load warrants stopping, abort and dispose of only this partial copy under the approved cleanup plan. Do not pin a source hardlink or suspend retention to preserve the artifact.
4. **Inspect schema requirements privately, restore everything.** Within the confined target, inspect the archive TOC and schema dependencies without exposing SQL/object comments/records in chat. Verify PostgreSQL compatibility, extension requirements and available trusted binaries; stop on unsupported dependencies rather than skipping data or executing an unreviewed extension. Create a fresh database from `template0`; do not use an existing database, `--clean` or `--create`. Prospective command inside the isolated target: `pg_restore --host=<task>/socket --port=55447 --username=<scratch-role> --dbname=loyalty_restore_20260911 --exit-on-error --single-transaction --no-owner --no-acl <task>/archive.dump`. The role/ownership mapping is local-only. One job avoids the incompatibility between parallel jobs and single-transaction restore. Capture any potentially sensitive diagnostics privately; report sanitized phase/exit/SQLSTATE only, never COPY failure rows or SQL literals.
5. **Meaningful recovery acceptance.** Require checksum match and full restore exit **0**, with no omitted data sections, suppressed errors or disabled constraints. In explicit read-only transactions on the restored database, compare expected TOC object categories/counts with restored catalogs, inspect invalid indexes/unvalidated constraints and sequence restoration, and collect only aggregate table counts/size metadata in the private record. Do not emit contact/customer/order rows or run restored application functions. A database restart and repeat catalog/aggregate checks must succeed. Record elapsed time, peak storage/RAM and complete failure status if any. No source-row comparison or historical row-count equality is claimed without an existing compatible baseline; no current production query is part of this plan. Owner/ACL recreation, point-in-time/WAL recovery, complete application/credential recovery and off-host-provider recoverability are separate limitations of this logical rehearsal.
6. **Disposal and evidence.** On success or failure, stop only the exact scratch PostgreSQL process/cgroup and remove only the verified task-owned archive, cluster/WAL/temp files and private data-bearing diagnostics. Confirm process/socket closure and exact-path cleanup. Retain only hashes, artifact stats, tool versions, timestamps, aggregate verification results and sanitized exit codes in the release record. Proposed confidential retention is **the rehearsal duration only**, with teardown immediately on completion/abort; unexpected interruption requires cleanup before any resume. Deletion is irreversible for the disposable copy and does not guarantee physical erasure from SSD/VHD snapshots, pagefiles or external backups; protected storage and capture/disposal policy must therefore be verified first. Never delete or alter Green's backup, sidecar, timer, helper, app or database.

The [PostgreSQL 17 pg_restore contract](https://www.postgresql.org/docs/17/app-pgrestore.html) confirms that restores execute source-controlled SQL, supports fail-fast/single-transaction restoration, and makes archive listing distinct from data restoration. Filesystem/network confinement is consequently required even though this is our own trusted-source backup. The [SQL-dump recovery documentation](https://www.postgresql.org/docs/17/backup-dump.html) also separates database restoration from cluster-wide roles/settings; the proposed ownership remapping does not verify those production recovery requirements.

**Effects to approve:** a confidential production-backup export to this protected local target, temporary local database/data/resource creation and execution of its restore SQL, bounded Green read/network load, and irreversible deletion of only the disposable local copy at completion. No production data mutation, backup-helper upgrade, service restart, off-host provider access, paid service, application launch, deployment or activation is included. Verify storage/isolation gates before transfer. **Authorization boundary:** this pinned-artifact local copy/restore/cleanup would require separate approval after all empty-target prerequisites pass, or existing valid actual-restore evidence could satisfy the recovery gate instead. The preflight below remains blocked on protected-storage evidence; no export approval is requested while that prerequisite is unresolved. No cashier-policy reapproval is needed. Off-host copy verification remains a distinct gate; the helper provenance concern itself is now explained and does not require an upgrade to proceed with this plan.

## Empty-target preflight — synthetic local controls verified; export still blocked

September 11, 2026 follow-up used **local WSL only**, tiny synthetic files and auto-collected per-task units. No Green connection, production backup/data access, confidential-target creation, package installation, elevation prompt, global configuration or existing-service change occurred. Candidate code remains unchanged; no repository test suite was rerun.

| Required preflight control | Exact result |
|---|---|
| Confidential target absent | **PASS**, before and after probes: `/home/jeffr/degen-restore-rehearsals/green-20260911` does not exist, including no dangling symlink. It was not created. |
| Private namespaces | **PASS**: `unshare --user --map-current-user --keep-caps --mount --net --pid --fork` created separate user/mount/network/PID namespaces without host elevation. Final confined process UID **1000**, mapped to host UID **1000**; setup capabilities were dropped and `no_new_privs=1` verified before the runtime probe. |
| Filesystem confinement and access | **PASS**: private root exposes selected Python/PostgreSQL binaries and libraries read-only, a read-only synthetic archive and writable synthetic scratch. Host-home and Windows paths and an outside synthetic canary were inaccessible. Archive/runtime mount read-only flags were verified and writes rejected. Scratch directory/file permissions were **0700/0600**. Only synthetic fixture contents were examined. PostgreSQL **17.9** `pg_restore --version` ran successfully inside this confinement; this does not claim a real archive/extension restore. |
| Network isolation | **PASS**: the private network namespace contained only loopback and no external route. A bounded connection attempt to documentation-only **192.0.2.1:9** failed **ENETUNREACH**, with no host-network connection. No TCP listener, tunnel, production API or credential access was used. |
| Per-task resource controls | **PASS** on cgroup v2: user transient unit applied/read back **memory.max=8589934592**, **memory.swap.max=0**, **cpu.max=200000 100000** (8 GiB, no cgroup swap, 2 CPU equivalents). The probe allocated only 1 MiB; no load/stress test or 8 GiB allocation was performed. Filesystem/runtime probe also ran under those limits. Units ended **inactive / LoadState=not-found** after automatic collection. This verifies the Linux task controls, not Windows host pagefile or hibernation protection. |
| Measured-storage abort | **PASS** at synthetic scale: watchdog detected **262,144 bytes** and terminated only its writer process group; final size **262,144 bytes**, no writer left running. The production-size proposal remains a measured-usage watchdog with headroom, **not a verified hard disk quota**. No large file or actual transfer was used to test it. |
| C: protection metadata | **BLOCKED / UNVERIFIED**: both `Get-BitLockerVolume -MountPoint C:` and CIM `Win32_EncryptableVolume` for C: returned **PermissionDenied / HRESULT 0x80041003**; `manage-bde.exe -status C:` returned **exit 3, access denied**. All were noninteractive/read-only with no elevation attempt. These errors do **not** establish that encryption is disabled. |
| Remaining storage/capture prerequisites | **UNVERIFIED**: protected Windows backing storage, host swap/pagefile/hibernation handling and unintended automatic backup/sync capture remain the existing plan's prerequisites. They are not established by namespace isolation or `memory.swap.max=0`. No new security policy or credential requirement is introduced by this preflight. |
| Teardown/preservation | **PASS**: all **six** uniquely identified synthetic scratch directories were removed after child exit; no scratch mount remained in the parent namespace, and per-task units were collected. Existing services/previews, source worktrees and the confidential target were untouched. All **84** candidate content hashes still match the prior manifest. |

Reproducible synthetic probe scripts and exact evidence: `/tmp/loyalty-empty-preflight.py`, `/tmp/loyalty-empty-preflight-result.json`; `/tmp/loyalty-empty-filesystem.py`, `/tmp/loyalty-empty-filesystem-result.json`; `/tmp/loyalty-empty-preflight-encryption.json`; `/tmp/loyalty-empty-preflight-cleanup.json`. Early filesystem probe attempts failed because the temporary harness did not preserve inherited mount flags, mounted its root after child mounts, or required EROFS where normal permission denial also rejected a write. These were corrected **only in the temporary synthetic harness**, and the final probe verifies read-only mount flags plus rejected writes. Intermediate results remain in `/tmp/loyalty-empty-filesystem-*-result.json`; no production workaround or weaker confinement was used.

**Minimal next action:** parent/user supplies read-only Windows C: encryption/protection evidence from an authorized administrator context or existing Device Encryption/BitLocker UI, without sharing recovery keys or changing encryption, plus confirms the existing plan's backing-storage/swap/capture protections. For example, a parent-run `manage-bde -status C:` can establish volume/protection status where this non-elevated session was denied. If the controls are absent, choose an already approved protected target or separately review a remedy; do not silently enable encryption, install tooling or change Windows settings. **Do not request or perform the actual-data copy yet.** Once these existing storage prerequisites are verified, the concrete pinned-artifact copy/restore/cleanup proposal above can be presented for approval. No further namespace/resource capability investigation is needed absent environment changes.

## Delivered behavior and source map

| Area | Implementation | Review result |
|---|---|---|
| Exact earning/refunds | `app/loyalty/domain.py`, `shopify_reader.py` | Whole points from cumulative net eligible POS merchandise, exact cents, all discount allocations, inclusive-tax treatment, successful refund evidence, gift issuance exclusion and gift tender support. Ordinary returns can correct points; exchanges and ambiguous evidence require review. |
| Persistence and recovery | `app/loyalty/models.py`, `schema.py`, `service.py`, `reconciliation.py`, `worker.py` | Additive BIGINT account/order/ledger/inbox/reconciliation storage; constraint/index/trigger readiness; delivery and business idempotency; conflict-safe inserts, leased fencing, stale evidence protection and transaction recovery. POS-filtered overlapping scans plus recovery of known orders. |
| Intake | `app/routers/shopify_loyalty_webhooks.py` | Dedicated bounded raw-HMAC receiver, minimized durable receipt before success, exact shop/topic allowlists, independent receiving gate. No raw contact payload retention. |
| Staff Ops | `app/routers/loyalty.py`, `app/loyalty/access.py`, `ops.py`, `app/templates/loyalty.html` | ID-based balance/history; explicit grants; current session validation; CSRF and AuditLog; bounded correction/replay; audited attachment verification before an account exists; readable mobile cards and restricted diagnostics. |
| Narrow integration | `app/config.py`, `db.py`, `main.py`, `permissions.py`, `shared.py`, `app/templates/_linear_sidebar.html` | Three flags default OFF, schema registration before initialization, permission defaults and sidebar integration. Lightweight users or permission lookup failures cannot break unrelated pages. |
| Local preview | `scripts/loyalty_demo.py`, `loyalty_demo.html`, `loyalty_synthetic.py`, `tests/test_loyalty_demo.py` | Localhost-only wrapper around actual routes, synthetic role/sample picker, real browser HTTP interactions, scratch database/home, no production app lifespan or external providers. Demo login is not registered by the production app. |
| Verification | `scripts/run_loyalty_tests.py`, `tests/test_loyalty_*.py` | Isolated environment, synthetic HOME/USERPROFILE/LOCALAPPDATA/APPDATA, disabled dotenv/providers, blocked external sockets, optional isolated PostgreSQL and browser checks. |

Shared Shopify Admin API stays on `2026-04`. Content comparison against the base confirms `app/shopify_api.py`, `app/inventory/shopify_ingest.py`, `app/routers/tiktok_orders.py`, and `app/tiktok/tiktok_ingest.py` are unchanged. Smile, inventory webhook behavior and TikTok signing are outside this feature.

## Walkthrough and tests

The exact launcher command, URL and staff steps are in [staff-walkthrough.md](staff-walkthrough.md). Default URL: **http://127.0.0.1:8766/demo**. Launching the actual script with an ephemeral port returned HTTP 200; that smoke server was stopped. Each subsequent launch starts fresh synthetic examples.

Live-browser verification uses a real Uvicorn listener and actual HTTP forms, not intercepted static HTML. It checked cashier/admin selection, earning, cumulative refund history, pending attachment, exchange review, correction/AuditLog, replay persistence, correction-hold preservation and denied cashier mutation. Screenshots were opened and inspected:

- `verification/demo-cashier-desktop.png`
- `verification/demo-cashier-mobile.png`
- `verification/demo-admin-desktop.png`

Historical verification evidence (prior tasks; not rerun as a full repository suite):

| Check | Result / log |
|---|---|
| Final feature, Ops/endpoints, home-path and live-browser group | **155 passed, 1 skipped**, 19.89s; `/tmp/loyalty-feature-release-final.log`. Skip: PostgreSQL-only corruption test in the SQLite invocation. |
| Home-path correction | All 9 MCP smoke cases passed alongside 20 loyalty Ops cases in the focused invocation; the prior `Path.home()` failure is resolved by synthetic home assignment. |
| Existing feature coverage from preceding review | 140 focused tests passed, 1 skipped; PostgreSQL/SQLite service/lifecycle 78 passed, 1 skipped. Core ledger/reader source is unchanged by this walkthrough task. See `runbook.md` for exact prior logs. |
| Full repository run, completed | **3,931 passed, 221 failed, 89 skipped, 191 subtests passed**, 1,759.84s (29m19s); `/tmp/loyalty-full-release-final.log`. This run reached completion; it was not truncated. |
| Failed-case verification after isolation fixes | **221 passed, 1 failed, 48 subtests passed**, 83.44s; `/tmp/loyalty-failure-focused-release-second.log`. Includes all original 221 failed cases plus the browser fixture that triggered the cascade. |
| Clean-base comparison | Base `cbc7896`, code-only disposable copy under `/tmp/loyalty-clean-base-4m75ihwx`, same harness. Browser startup/asyncio cascade reproduced before browser setup repair; remaining Clockify failure reproduced with the final harness. Logs: `/tmp/loyalty-baseline-release-repro.log`, `/tmp/loyalty-baseline-release-final.log`. |
| Final demo recheck after copy cleanup | **1 passed**, 5.35s; `/tmp/loyalty-demo-release-final.log`; screenshots regenerated and inspected. |
| Compile / whitespace | Compile of app and new launcher/isolation scripts passed; `git diff --check` passed. |

The full run exposed three isolation issues: an explicit `MEDIA_ROOT` override broke its intended inheritance test; synthetic home paths hid installed Chromium/WebKit binaries; and the network block initially denied other tests’ own loopback servers. These are repaired in the test harness. Browser startup failure in an existing fixture left an asyncio loop active and caused most later failures. The final failed-case run uses explicitly selected existing browser binaries and permits only loopback sockets bound by this process, while blocking external browser requests and retaining a synthetic home/profile. No unrelated application or test source was changed.

At that historical checkpoint, `tests/test_employee_hours_consistency.py::EmployeeHoursConsistencyTests::test_week_bounds_cover_monday_to_sunday` failed because its empty-cache fixture called Clockify without a mock. The network guard rejected it on both clean base and the original feature source. The new current-main candidate fixes only that test fixture and passes the required full suite as recorded above; the preserved original source was not backported. The earlier failed run remains historical evidence.

Those historical Windows checks used Python 3.12.10 and the earlier disposable PostgreSQL 16 checks below. Green was subsequently verified as Python 3.11.15/PostgreSQL 17.9; the current-main candidate now passes isolated checks on those versions. One dependency deprecation warning remains; accounting source is unchanged.

## September 11 automated readiness verification

All 12 `tests/test_loyalty_*.py` files were exercised across the existing Windows/WSL interpreters through `scripts/run_loyalty_tests.py`. Tests use scratch databases/homes, synthetic signing keys, disabled dotenv/external providers and blocked external sockets. Store, POS API and lifecycle fixtures run on both SQLite and disposable PostgreSQL; Ops/native-preview/browser fixtures use SQLite. No live orders or customer records were read.

| Current check | Result / exact evidence |
|---|---|
| SQLite domain, reader, receiver, configuration/app wiring, Ops, POS API, service and lifecycle (Windows Python 3.12.10) | **174 passed, 2 skipped**, 16.74s; `/tmp/loyalty-readiness-sqlite.log`. Skips: opt-in demo browser (run separately below), PostgreSQL-only type corruption on the SQLite fixture. |
| Actual Ops HTTP walkthrough plus desktop/mobile permission/diagnostic rendering | **2 passed**, 7.04s; `/tmp/loyalty-readiness-ops-browser.log`. Includes denied cashier mutation, audited correction/replay and attachment examples. |
| SQLite + disposable PostgreSQL 16 domain/reader/receiver/service/POS API/lifecycle, plus all 15 native-backend tests (WSL Python 3.12) | **236 passed, 1 skipped**, 43.53s; `/tmp/loyalty-readiness-postgres-final.log`. Sole skip: PostgreSQL type-corruption test's SQLite variant; PostgreSQL variant passed. Own Unix-socket-only cluster was stopped; parent TCP server was not used. |
| Extension component checks | **32 passed**, 48.68s; `/tmp/loyalty-readiness-components.log`. Added 401/403/409/429 refresh recovery and customer switching while token acquisition is pending. Existing exact integer, stale response, session/location/PIN change, offline clearing/reconnect, timeout and malformed-response cases pass. |
| Actual synthetic POS browser, desktop/mobile | **2 passed**, 3.9s; `/tmp/loyalty-readiness-pos-browser-windows.log`. Includes customer switch while offline, reconnect into the new customer, history paging, visible review guidance, no overflow, and foreign Host/Origin denial. Existing Windows loopback 8767 was preserved. |
| Typecheck / bundle / compile / whitespace | Passed; `/tmp/loyalty-readiness-types-final.log`, `/tmp/loyalty-readiness-build.log`, `/tmp/loyalty-readiness-compile.log`; targeted whitespace check passed. Installed POS SDK remains 2026.7.0; no dependency changes. |

Environment issues are not hidden: the initial full WSL collection hit missing `bcrypt` in the small existing venv; an initial broader PostgreSQL invocation had **238 passed, 1 skipped, 1 failure** at the same app-import dependency (`/tmp/loyalty-readiness-postgres.log`). That app-initialization check passed in the Windows group; the final PostgreSQL invocation excludes the already-covered configuration module. No unrelated dependency installation or application fix was made. WSL's direct browser invocation had **2 connection-refused failures** because 8767 is reachable through Windows loopback, not this WSL namespace; the same tests passed from Windows. New test portability/type checks exposed a mock argument annotation and unnecessary Node built-in type imports; both were fixed, using Playwright’s own per-test evidence path. These are not production integration results.

Browser evidence was regenerated and opened: `C:/Users/jeffr/AppData/Local/Temp/degen-loyalty-tests-p4yawmn3/pos-browser/results/preview-synthetic-componen-544c2-switching-and-mobile-layout/loyalty-pos-presentation/{desktop,mobile,block-desktop,block-mobile}.png`. The browser host approximates native layout with actual extension components and the actual bearer route; it does not prove iOS lifecycle behavior. Basic native balance/history already has user evidence, so no repeat phone walkthrough is requested in this review.

Review coverage includes half-up cumulative net rounding, cash rounding/failed payment retry, successful ordinary refunds, ambiguous/exchange review, gift issuance versus gift tender, strict POS/location/prospective cutoff, seven-day authoritative attachment, failed merge versus successful-merge holds, delivery/business dedupe, first-insert concurrency, lease fencing, corrections/replay sharing the order lock, stale canonical protection, overlapping reconciliation/cursor-cycle recovery, BIGINT/index/check corruption and independent default-OFF/schema gates. Existing cases cover these; no speculative accounting changes were introduced.

## Approved policy remains settled

No new decision is requested about whole-point earning, seven-day attachment, gift-card issuance/tender, POS-only eligibility at launch, permanent TikTok exclusion, Shopify online exclusion at launch (with possible later inclusion), cumulative refund differences, no historical credit, or the absence of redemption/marketing/tiers/expiration. Shopify customer ID per shop remains account identity. Existing customers are attached in native POS using email or phone; no contact-presence rule is added.

Canonical association observed within the seven-day deadline is authoritative upper-bound evidence. Unsigned webhook headers, generic `updatedAt`, and delayed receipt dates cannot backdate attachment. Audited manual proof still runs the entire earning calculator and queues a fresh canonical check. Failed/pending merges do not freeze accounts. Successful merges and observed reassignments require manual review without automatic identity transfer.

## Next readiness stage — disabled deployment proposal, not execution approval

September 11 follow-up authorization covers read-only integration/production investigation and preparing this proposal. **No implementation changes or test reruns were needed in this stage.** Only this packet was updated; dirty feature work, other worktrees and the native/local previews were preserved. The first access attempts below did not execute a production command. The later authenticated inspection supersedes that boundary: only read-only host/catalog/Shopify identity metadata was inspected; no customer/order rows, app startup or production mutation occurred.

### Evidence obtained and access boundary

| Item | Confirmed result / remaining uncertainty |
|---|---|
| Original preserved feature worktree | `codex/shopify-pos-loyalty-20260910` at base **cbc7896fd054881ce1620b3e5ffa7051040da535**, with the existing uncommitted feature files. **There is no release-candidate commit or deployable approved SHA.** |
| Cached remote comparison | Local `origin/main` is **f40bb83d2b5585a029cbfb0808e907bd7267ffdc**, two commits ahead: `bf74f33` (#38) and `f40bb83` (#39), TikTok listing changes. This ref was not freshly fetched; the separate host inspection below now confirms the deployed SHA matches it. The later additive candidate above now applies the reviewed feature to this exact live origin/main SHA and passes its full suite; no merge/rebase/fetch/cleanup touched the original worktree. WSL's Windows-worktree “prunable” labels were not acted on. |
| Verified Green target — latest inspection | Authenticated Brev identifies **openclaw-9902ae / vmc5a2549 / RUNNING**; the existing managed connection reached **brev-vmc5a2549**, with app realpath **`/opt/degen/app`**. Current checkout, service/runtime, process-flag and catalog observations are recorded below. Source defaults remain distinct from deployed state. |
| Earlier sandboxed WSL attempts | Existing Brev **v0.6.326** first failed to create its SSH control socket under read-only `/home/jeffr/.ssh`. Subsequent strict host/default attempts failed host-key verification. Brev then emitted a generic login fallback; that fallback alone did not prove an authentication problem. Independent parent control-plane evidence below now establishes the reauthentication requirement. No remote command ran. Historical logs: `/tmp/loyalty-green-access.log`, `loyalty-green-access-safe.log`, `loyalty-green-access-default.log`. |
| September 11 unsandboxed bounded retry | One `brev exec openclaw-9902ae --host` attempt, bounded to **45 seconds** overall and **10 seconds/one SSH connection attempt**, exited **1**. SSH used existing `/home/jeffr/.ssh/known_hosts`, `StrictHostKeyChecking=yes`, `UpdateHostKeys=no`, batch mode and no connection sharing. Exact cause: **“No ED25519 host key is known for 136.109.4.119 and you have requested strict checking. Host key verification failed.”** At that earlier checkpoint, the endpoint address came from cached Brev resolution and its identity had not been independently verified. No remote metadata script executed. That retry's login prompt alone was generic fallback, not an authentication diagnosis; the later independent parent evidence below supersedes the earlier conclusion that sign-in need was unestablished. Log: `/tmp/loyalty-green-readonly-retry.log`. |
| Parent independent control-plane evidence | Parent ran **`wsl -d Ubuntu-24.04 -- brev ls`**, independently of SSH. It failed through **GetAccessToken → GetFreshAccessTokenOrLogin → PromptForLogin → EOF**: Brev could not obtain a fresh access token and requested login. This established the earlier reauthentication requirement, separately from the worker's SSH failure. **User subsequently completed login; authenticated listing now succeeds.** |
| Parent cached SSH policy evidence | Parent ran **`ssh -G openclaw-9902ae`** and observed cached hostname **136.109.4.119**, **StrictHostKeyChecking false** and **UserKnownHostsFile /dev/null**. Neither Windows nor WSL known-hosts files contain the hostname/IP. The worker's strict wrapper deliberately used a different trust policy from existing Brev configuration; its factual failure does **not** show that a previously populated managed trust setting newly broke. Cached resolution is not verification of current instance identity, and this evidence does not authorize bypassing host verification. |
| Login and connection — completed | User completed **`wsl -d Ubuntu-24.04 -- brev login`**. The worker then verified `brev ls --json` and used the **existing legitimate Brev managed connection** successfully, without the earlier strict wrapper, changing SSH/credential configuration or bypassing an actual rejection. Login/trust repair is no longer a pending action. |
| Shopify integration — verified identity and installed metadata | **Degen Ops / 346302742529**, Shop **75779080408**, canonical **gsjtfr-r6.myshopify.com**, configured alias **degencollectibles.myshopify.com**. Parent verified both domains connected under the intended production store; the alias discrepancy is resolved. The follow-up query verified installed scopes and a complete two-subscription API-created webhook inventory below. `read_returns` is absent. DEV app **422232031233** / Shop **81339547671** remain excluded; runtime launch configuration remains unset. |

The unsandboxed retry changed no credential, profile, known-host entry or production state. Only temporary local probe files and this packet were written. At that earlier checkpoint, live SHA, services, runtime, flags, schema and backups were unverified. The authenticated inspection below supersedes those unknowns to the precise extent shown; candidate source defaults remain OFF. During that earlier failed-access stage, no tests, app initialization, migration, restart, customer/order read, export or Shopify request was performed. The approved sole retail location **83278725336** is unchanged; owner/budget remain unanswered and production loyalty Shop GID configuration/launch cutoff remain unset.

### Authenticated read-only production inspection — September 11, approximately 20:32 UTC

This is **worker-observed production metadata**, distinct from the earlier failed strict-wrapper attempts and parent browser location evidence. `brev ls --json` exited 0 and returned the exact RUNNING instance **openclaw-9902ae / vmc5a2549**. Bounded `brev exec ... --host` calls then succeeded through the existing managed connection. No login, SSH/profile/credential configuration change, production file write, service restart, app import/startup or migration was performed. Temporary probe scripts/logs stayed local under `/tmp`.

| Verified live fact | Evidence / implication |
|---|---|
| Host and repository | **brev-vmc5a2549**, `/opt/degen/app`; canonical remote match true. Branch **main**, HEAD **f40bb83d2b5585a029cbfb0808e907bd7267ffdc**. Tracked/index clean; full status reports **0 tracked changes and 0 untracked entries**. Matches the feature worktree's cached origin/main; no fetch/pull occurred. |
| Web / worker / Ops bot | **degen-web.service** active/running, PID **2065033**, started **18:15:33 UTC**; **degen-worker.service** active/running, PID **2065051**, started **18:15:35 UTC**; user-scoped **degen-ops-discord-bot.service** active/running, PID **2065234**, started **18:15:45 UTC**. All report working directory `/opt/degen/app`; web/worker run as `degen`. These service states do not prove background business-work throughput. |
| Actual runtime/dependencies | Live venv and web/worker interpreter: **Python 3.11.15**. Installed: FastAPI **0.136.1**, SQLModel **0.0.38**, SQLAlchemy **2.0.49**, psycopg **3.3.3**, PyJWT **2.12.1**. The candidate requires PyJWT **2.13.0**. Prior local checks used Python 3.12; **isolated candidate compatibility with this Python runtime is now verified in the current-main section above**. No dependency was installed or changed here. |
| Loyalty code and flags | No deployed `app/loyalty` directory or any of the three loyalty router files. None of the five loyalty flag aliases exists in deployed `app/config.py`. All five are **unset in both running web/worker process environments**, not explicitly false. Current production does not contain this loyalty implementation; do not equate candidate OFF defaults with deployed configuration values. No dotenv or full environment dump was read/emitted. |
| PostgreSQL metadata | Used only the running web service's existing **local PostgreSQL** connection, with `default_transaction_read_only=on`, explicit read-only transaction, 3s statement/1s lock timeouts and rollback. Confirmed `transaction_read_only=on`, PostgreSQL **17.9** and **zero catalog relations** matching the five loyalty table names. No customer/order/account/ledger rows were read. Prior disposable verification used PostgreSQL 16; isolated PostgreSQL 17 compatibility/readiness verification is now complete for the candidate above; post-deployment catalog verification remains required. |
| Backup service / timer | Timer active/waiting; last trigger **2026-09-11 10:23:12 UTC**, next scheduled **2026-09-12 10:25:57 UTC**. Latest backup service invocation ran **10:23:12–11:37:11 UTC**, `Result=success`, `ExecMainStatus=0`; inactive/dead afterward is normal for this completed oneshot. |
| Backup artifacts — stat only | `/opt/degen/backups/db` contains **2 dumps**, both with `.sha256` sidecars present. Newest mtime **2026-09-11 11:06:57 UTC**, **21,177,519,272 bytes**, age **33,935 seconds** at observation; previous mtime **2026-09-10 11:05:36 UTC**, **21,162,575,093 bytes**, age **120,416 seconds**. No dump/sidecar contents were opened. This does **not** verify checksums, off-host copies or logical restore readiness. |

Logs: `/tmp/loyalty-green-managed-metadata.log`, `/tmp/loyalty-green-details.log`, `/tmp/loyalty-green-final-metadata.log`. The host-side process-environment filter retained only exact flag/connection/transport keys in memory, returned flag states/presence booleans, and never emitted connection strings or credential values. Database work was catalog-only and rolled back.

The bounded Shopify identity probe reused the deployed transport's four reviewed pure request functions, comparing normalized function source with this checkout and compiling only those definitions in memory. It **did not import the app, load Settings/dotenv or initialize modules/workers**. The credential stayed host-side in memory from the existing running service. One initial local/probe AST comparison differed across Python 3.12/3.11 representations and stopped before any API call; normalized source comparison then passed. No production source was edited.

The read-only `shop` / `currentAppInstallation` query used the existing **2026-04** transport. It returned:

- Request domain: **degencollectibles.myshopify.com**.
- Canonical returned domain: **gsjtfr-r6.myshopify.com**, USD; observed Shop GID **gid://shopify/Shop/75779080408**.
- Installed app: **Degen Ops / gid://shopify/App/346302742529**, matching the expected production app identity and distinct from DEV.

At the earlier checkpoint, the exact-domain guard stopped webhook inventory; the bounded identity follow-up retained only non-secret identity evidence. Historical logs: `/tmp/loyalty-green-shopify-metadata-final.log`, `/tmp/loyalty-green-shopify-identity.log`. The parent evidence and scoped follow-up below resolve that discrepancy without changing shared inventory configuration.

### Parent domain confirmation and installed metadata follow-up — September 11

Parent authenticated browser evidence at [Degen Collectibles Domains](https://admin.shopify.com/store/degencollectibles/settings/domains) shows primary **shop.degencollectibles.com — Connected**, with both **gsjtfr-r6.myshopify.com — Connected / domain ID 125750214872** and **degencollectibles.myshopify.com — Connected / domain ID 127316033752** nested under the same store. This independently confirms that the API-returned canonical domain and existing request alias belong to the intended production shop. **The alias discrepancy is resolved; no further alias-confirmation action is pending.** This evidence does not change runtime configuration or authorize deployment/activation.

The worker reverified authenticated Brev instance **openclaw-9902ae / vmc5a2549 / RUNNING**, then used the existing managed connection and reviewed **2026-04** Admin transport. One identity/scopes query required exact request domain **degencollectibles.myshopify.com**, canonical domain **gsjtfr-r6.myshopify.com**, Shop **gid://shopify/Shop/75779080408**, and app **gid://shopify/App/346302742529 / Degen Ops** before continuing. All guards passed; current installation is **gid://shopify/AppInstallation/668100886744**. No app initialization, customer/order query, mutation, credential output or runtime configuration change occurred.

| Installed-access evidence | Result / implication |
|---|---|
| Complete `currentAppInstallation.accessScopes` handles | `read_all_orders`, `read_customers`, `read_inventory`, `read_orders`, `read_products`, `write_customers`, `write_inventory`, `write_orders`, `write_products` (**9**). Existing write grants were observed only; no write operation was used or authorized. |
| Earning reader prerequisite | **`read_returns` absent**, confirming the earlier configured-scope concern against actual installed access. The implemented Return/exchange reader needs that prerequisite resolved through separately authorized app scope/install changes before live canonical acceptance/processing. Do not remove return safety checks to accommodate the current grant set. |
| Customer/location and protected-data limits | `read_customers` is installed; **`read_locations` absent**. Validate the exact minimum location-field access needed by the canonical query and protected-data approval independently; these metadata reads do not prove real order/customer/retailLocation field access. No blanket scope expansion is approved. |
| Older recovery | **`read_all_orders` and `read_orders` are installed.** The older-order scope is no longer an unknown or missing installed grant. This does not authorize historical credit or an order-data probe; postlaunch recovery and prospective cutoff rules remain unchanged. |

One bounded webhook page (`first: 50`, maximum two pages) returned **2 subscriptions**, `hasNextPage=false`; the API-created inventory is complete for this app/shop. Only IDs, topics, API versions and endpoint-match booleans were retained; callback URLs, query strings and credential values were not emitted.

| API-created subscription GID suffix | Topic | API version | Endpoint evidence |
|---|---|---|---|
| **1644180209880** | `ORDERS_CREATE` | **2026-04** | HTTPS, host matches `ops.degencollectibles.com`; exact proposed loyalty URL match **false** |
| **1644180242648** | `ORDERS_UPDATED` | **2026-04** | HTTPS, host matches `ops.degencollectibles.com`; exact proposed loyalty URL match **false** |

**No API-created subscription targets `https://ops.degencollectibles.com/webhooks/shopify/loyalty`.** The existing two subscriptions remain untouched. This query does not inventory app-scoped TOML subscriptions, other apps' subscriptions, or prove delivery health. The selected production app's released configuration and signing ownership still need review before any separately approved loyalty subscription changes. The pinned [WebhookSubscription fields](https://shopify.dev/docs/api/admin-graphql/2026-04/objects/WebhookSubscription) and [QueryRoot webhookSubscriptions contract](https://shopify.dev/docs/api/admin-graphql/2026-04/objects/QueryRoot) were checked for this metadata-only request; no shared API version was changed. Safe evidence log: `/tmp/loyalty-green-grants-webhooks.log`.

Production Shop GID **75779080408** and the domain relationship are verified facts; loyalty runtime settings remain unset. Final POS token `dest`/issuer, webhook shop-header and Admin-reader configuration must use the reviewed production identity consistently. Exact signing-app selection, protected-data/return access and provisioning remain launch gates; DEV identifiers must not be substituted.

**Remaining disabled-release gates:** no release-candidate SHA yet; review the additive current-main candidate and revalidate if main subsequently changes; its full suite and Python 3.11/PostgreSQL 17 compatibility now pass with the documented test-only fixes; establish checksum/off-host/restore evidence; approve the exact normal-deploy dependency/configuration/restart scope. Actual Green access and baseline metadata are now verified and should not be requested again as though still blocked. No tests were rerun in this metadata-only stage.

### Parent live browser follow-up — sole retail location approved

Parent evidence supplied September 11 in `loyalty-live-parent-evidence.txt` is independent of the worker's failed Green SSH/Brev attempts. Shopify admin for `/store/degencollectibles/settings/locations` displayed **Using 1 of 10 active locations**, with only **2266 Senter Road — gid://shopify/Location/83278725336** active. Jeffrey confirmed this is the only retail location and approved its inclusion as the **sole production retail location in the launch plan**. No further location-selection approval is pending.

Parent opened `/settings/locations/83278725336`: the POS section displayed **Subscription None**. The POS subscription page `/apps/point-of-sale-channel/settings/manage-subscriptions` likewise showed only this location, with **Plan —**. These are verified UI observations, not proof of a Pro subscription or a Pro requirement. Do not infer a required upgrade or change a subscription from them.

**Production loyalty Shop GID configuration and launch cutoff remain unset**; Shop GID **75779080408** and its canonical/request domain relationship are verified above; no alias confirmation remains pending. The cutoff is intentionally deferred until activation. Exception owner and spending budget remain unanswered. The parent browser evidence is separate from the successful host/catalog inspection above; installed API grants are now inventoried above, with `read_returns` absent; remaining deployment gates are unresolved. No production/configuration changes or activation occurred; all source flags remain OFF.

### Exact proposed disabled backend change

The proposed destination is the normal Green repository release path, not a copy of this dirty worktree. `RELEASE_SHA` remains **UNSET** until an authorized candidate is committed/reviewed. The latest observed rollback baseline (`PREVIOUS_GREEN_SHA`) is **83f18502fd27f89272e35f0e12c35cc6d3986133**, superseding the earlier f40bb83 observation, and must be reverified immediately before any approved deployment. No executable deployment approval can be given against the base SHA as though it contained this feature.

| Surface | Proposed scope / effects |
|---|---|
| Existing files | Loyalty hunks in `app/config.py`, `app/db.py`, `app/main.py`, `app/permissions.py`, `app/shared.py`, `app/templates/_linear_sidebar.html`; `requirements.txt` adds **PyJWT==2.13.0**. Preserve all non-loyalty work and newer main content. |
| New backend files | `app/loyalty/{__init__,access,config,domain,models,ops,pos_auth,queries,reconciliation,schema,service,shopify_reader,worker}.py`; `app/routers/{loyalty,loyalty_pos,shopify_loyalty_webhooks}.py`; `app/templates/loyalty.html`. Test/runbook/extension source can accompany the reviewed repository candidate, but demo/native launchers must never become production service entrypoints. |
| Database startup | Registration before `SQLModel.metadata.create_all()` adds **five tables**: `loyalty_account`, `loyalty_entitlement`, `loyalty_ledger`, `loyalty_inbox`, `loyalty_reconciliation`. Source declares **17 field indexes**, **6 named unique constraints**, **9 named checks**, primary/foreign keys and BIGINT money/point columns. PostgreSQL also installs `loyalty_ledger_immutable()` and its UPDATE/DELETE rejection trigger. This happens at normal app initialization **even with loyalty OFF**. Do not call initialization as a read-only probe. No existing customer/contact column or backfill is added. Partial DDL failures require catalog review; health alone does not prove readiness. |
| Permissions/startup rows | Insert missing defaults for `ops.loyalty.view`, `admin.loyalty.reconcile`, `admin.loyalty.correct` across employee/viewer/manager/reviewer/admin (up to **15 rows**): admin true, other roles false, existing grants preserved. Admin Ops navigation/page becomes available; ordinary staff need explicit Ops grants. The separate all-cashier native read policy adds no per-person mappings and grants no correction rights. Existing normal startup migrations/fixups still run; disabling loyalty does not disable those shared effects. |
| Disabled runtime | Require effective **false** in every relevant process for `LOYALTY_RECEIVING_ENABLED`, `LOYALTY_PROCESSING_ENABLED`, `LOYALTY_POSTING_ENABLED`, `LOYALTY_POS_READ_ENABLED`, `LOYALTY_POS_ALL_STAFF_ENABLED`. Keep new app/shop/secret/location/cutoff/owner/retention/budget settings unset for this disabled release. No loyalty task starts, receipt commits, accounting postings or POS reads are enabled. Existing unrelated Shopify settings are not repurposed. |
| Routes/edge | Registers `/loyalty` plus authorized Ops actions; `POST /webhooks/shopify/loyalty`; `GET/OPTIONS /api/loyalty/pos/customers/{customer_id}`. Proposed eventual HTTPS base is the existing Ops domain **`https://ops.degencollectibles.com`**, subject to exact production app URL/proxy/auth review. Disabled API reads/receiver return 503; current Ops permissions remain enforced. No real-customer test request is needed to verify disabled routes. |
| Shopify/native | **No app configuration, scopes, subscription, install, extension bundle release or activation in this backend proposal.** POS extension release is a separate action against an explicitly selected production app. `shopify/loyalty-dev` and all DEV identifiers/secrets remain development-only. Shared Admin transport stays **2026-04**; POS extension stays **2026-07**. Inventory ingestion, TikTok signatures and Smile remain unchanged. |

### Deployment mechanism and rollback impacts to approve later

Source inspection of [Green deployment contract](../green-autodeploy-cutover.md), `.github/workflows/deploy.yml` and `scripts/redeploy-linux.sh` confirms that a **push to main automatically deploys** through the `self-hosted, degen-prod` runner. The workflow requires clean tracked main, fast-forwards to the triggering SHA, and invokes `DEGEN_EXPECTED_GIT_SHA=<that SHA> ./scripts/redeploy-linux.sh`. This is a prospective description, not a command executed here.

The current script also installs the whole Python requirements set, creates/adjusts log directories and ownership, writes a deploy stamp, synchronizes `NVIDIA_MODEL`, `PARSER_REPROCESS_ENABLED` and `PERIODIC_STITCH_AUDIT_ENABLED` in existing service configuration files, restarts **degen-web.service**, restarts **degen-worker.service** if present, and by default restarts **degen-ops-discord-bot.service** in its configured user scope if present. These are broader operational effects than “add a disabled loyalty page.” The final deployment approval must cover the verified actual scope or a separately reviewed narrower mechanism. This stage neither runs nor modifies the script. The generic Ops-bot preflight script is not the loyalty deployment approval contract.

Before an eventual main push: reconcile the candidate with then-current main, review the complete diff, complete required full-suite checks (the candidate now fixes the historical Clockify fixture issue without production-code changes), verify actual Green Python/dependency compatibility, record the actual previous SHA and dependency inventory, and capture the approved backup/recovery evidence. Do not rerun prior green feature tests merely for this documentation pass. Any later source merge/change requires appropriate renewed verification.

Rollback should follow the existing exact-SHA repository workflow using a targeted content revert/corrective commit that preserves deploy hardening and later unrelated changes. Keep the five tables, constraints, trigger, permission rows and any audit/history; do not reset production Git or restore an older database over later valid data. Verify all five flags OFF and restore compatible code/dependencies under the separately approved rollback plan. Reverting the feature's `app/db.py` registration/route integration removes application use of the added tables without destructive schema rollback. Native removal would require a separate Shopify release if an extension had subsequently been activated; none is included here.

### Read-only verification contract and remaining checks

Authenticated host/checkout/service/catalog inspection has now completed as recorded above. Revalidate the baseline at any later deployment window; remaining protected-data, released-app-configuration and backup checks retain the same bounded scope; the installed scope/API-created webhook inventory is recorded above. Do not import `app.main`, `app.db` or load Settings to inspect production, because those paths can load secret configuration or participate in startup behavior.

| Check | Bounded evidence to retain |
|---|---|
| Target/checkout/runtime | Host identity and app realpath; Git branch/HEAD and tracked-clean boolean with optional locks disabled; canonical remote **match boolean**, not raw remote URLs; service LoadState/ActiveState/SubState/MainPID/User/WorkingDirectory/start timestamps; interpreter version and selected installed dependency versions. No command-line/environment dumps or service logs. Actual web/worker/user-bot units and Python 3.11.15 runtime are now verified above; performance/worker throughput and future deployment state remain separate checks. |
| Effective disabled state | Filter only the five exact loyalty flag keys to `true`/`false`/`unset`/`invalid`; presence booleans only for required non-flag settings. Inspect each relevant live service through an approved host-side filter without emitting other environment bytes or importing the app. Unset is equivalent to OFF only after the deployed source default is confirmed. No secret-file reads or credential output. |
| Database catalog only | Use an existing authorized readonly connection with transaction read-only, a short statement timeout and rollback on exit. Return PostgreSQL version and presence/types/index/check/trigger metadata for the five exact loyalty names; permission-resource counts/role-grant booleans only if present. No account/order/customer/ledger rows. No migration, schema installer, app startup, `CREATE`, `ALTER`, `ANALYZE` or repair call. Current connection/DB identity and authorization must be verified first without printing a connection string. |
| Existing backup evidence | Inspect backup timer/service state and allowlisted non-secret successful-run/verification timestamps, installed asset-match booleans and approved recovery records. The [backup runbook](../green-postgres-backup-runbook.md) specifies a reviewed helper/archive process and explicitly says `pg_restore --list` is **not** restore proof; its full restore rehearsal was canceled in that historical record. Current timer/service success, local artifact age/size, bounded receipt inspection and an installed operations-helper hash mismatch are verified in the final review above. The synthetic restore passed; actual-production checksum validity, last valid off-host copy and completed restore proof remain unverified. Do not create a backup, run retention/probe/record helpers, `rclone`, restore a dump or copy customer data in this stage. |
| Post-disabled-deploy verification, only after deployment approval | Exact approved SHA, healthy expected services, five flags OFF, additive catalog/readiness checks and preserved permission grants; unauthenticated Ops denied, disabled POS/receiver 503, no loyalty worker scheduled. Read safe counters/catalog to confirm no loyalty receipt/posting activity. Check worker status separately from `/health`; the existing workflow health request alone is insufficient. |

### Shopify integration proof and remaining decisions

The existing reader uses `app.inventory.shopify.shopify_graphql_request` and the configured Admin transport. The metadata probe now verifies that transport belongs to **Degen Ops / 346302742529**, with verified Shop GID **75779080408**; the parent has independently confirmed the request/canonical domain relationship. Installed scopes and the complete API-created webhook inventory are now recorded above. Any later probe must recheck identity and stop on a DEV shop, mismatched production target or unknown app. Use only bounded metadata queries—no order/customer rows, bulk queries, mutations or exported data. If a later metadata refresh is needed, use one identity/grants query, then at most two pages of 50 location records and two pages of 50 relevant webhook records; a remaining cursor means the inventory is incomplete, not absent. Retain only IDs, location names/active status, scope handles, topic/API-version/approved endpoint-match booleans and completeness flags; redact URL query/userinfo components.

[Granted scopes](https://shopify.dev/docs/apps/build/authentication-authorization/manage-access-scopes) come from `currentAppInstallation.accessScopes`, not the app's configured-scope list. The [current installation](https://shopify.dev/docs/api/admin-graphql/latest/queries/currentappinstallation) is the authenticated app's installation and can expose its active app-billing subscriptions; those subscriptions do **not** establish a POS Pro location plan. [Webhook subscriptions](https://shopify.dev/docs/api/admin-graphql/latest/queries/webhookSubscriptions) list that app/shop's API-created shop subscriptions, not app-scoped TOML subscriptions or another app's webhooks. Parent live browser inspection of the location and POS subscription pages is recorded below; the selected production app's released TOML/configuration still needs separate verification. The displayed dash/None neither confirms Pro nor establishes that Pro is required. The executed webhook metadata fields were checked against the pinned **2026-04** reference; any additional prospective fields still require version-specific verification. No shared API upgrade is proposed.

Three bindings must be documented independently: **Admin reader token → installed app + production shop**, **loyalty webhook signing secret → subscription-owning app/shop**, and **POS bearer audience/signing secret → selected production POS app/shop**. No DEV credentials are copied, no existing inventory webhook secret is replaced to make another app work, and a zero-scope synthetic native preview does not establish earning-reader grants. Installed `read_orders`, `read_customers` and `read_all_orders` are verified. Resolve missing `read_returns` through separately approved setup, and verify minimum customer-ID/location field access and protected-data eligibility; `read_locations` is absent from the current grant set. Existing `customers/merge` topic access and protected-data eligibility also need actual verification before subscription approval.

| Decision / proposed choice | Status and gate |
|---|---|
| Production app | **Candidate only:** evaluate reuse of existing Degen Ops **346302742529** after installation/distribution/client/secret-owner checks. No selection or credential change is assumed; DEV app **422232031233** is excluded. A dedicated production app would need separate creation/setup approval. |
| Production location — approved | Jeffrey confirmed only one retail location and approved including it in the launch plan: **gid://shopify/Location/83278725336 — 2266 Senter Road**. Parent verified it is the sole active location. This settles the location selection without changing runtime configuration or approving activation; DEV **104761688087** remains excluded. |
| Exception owner and spending budget | Parent is collecting these decisions. Leave owner and integer-cent budget unset; require named review coverage and measured capacity (including the process-local POS limiter) before processing/posting approval. No duplicate policy questionnaire is needed. |
| Launch cutoff | **Intentionally unset until activation.** Set one approved immutable UTC cutoff then; require both order creation/processing to meet it. Disabled deployment does not require inventing a date. |
| Retention/recovery | Receipt/evidence/ledger/backup durations and an approved disposal/archive procedure remain unresolved. Recommended invariant: preserve non-expiring balances, ledger reconstruction and dedupe even when evidence is minimized. Existing backup retention rules are not automatically the loyalty retention policy. Full disposal and successful-merge release remain documented limitations. |
| Accounting integration | Metadata is necessary but insufficient: a later authorized canonical/shadow acceptance must cover discounts/tax/cash rounding, successful line refunds and authoritative attachment timing. This stage authorizes no order/customer reads or Shopify writes, so that evidence remains pending. |
| Settled staff/business policy | All cashiers read balances/history without extra login; corrections restricted in Ops; whole points, seven-day attachment, gift issuance exclusion/gift tender allowance, prospective POS-only launch and permanent TikTok exclusion remain approved. No reapproval requested. |

**Verdict:** the disabled-deployment proposal is concrete and reviewable, but is **not executable yet**. Authenticated Green access, clean deployed SHA, running services, actual runtime, absent loyalty tables and local backup metadata are verified. The release candidate SHA does not exist; current-main preparation/full-suite and Python 3.11/PostgreSQL 17 compatibility now pass for the isolated candidate above; backup integrity/restore proof and the exact deployment effects still need evidence and separate approval. Business launch inputs and actual accounting integration remain distinct activation gates. No deploy/push/commit/subscription/activation authority is inferred from this readiness work.

## Source-verified versus live-unverified

| Established evidence | Production evidence still needed |
|---|---|
| Exact arithmetic, domain cases, idempotency, SQL constraints/fencing, schema checks, permission enforcement and minimized projections | Missing `read_returns` and minimum field/protected-data access, additive schema readiness after an approved deployment, production app distribution and POS signing setup; current catalog has no loyalty tables and the existing Admin transport identifies Degen Ops |
| The reader queries documented `2026-04` fields and uses existing transport without mutations | Actual cash-rounding/refund/tax representations from this shop, canonical completeness for real orders, offline POS timing behavior |
| Return/exchange query requires `read_returns`; default order access has a 60-day horizon and older recovery needs approved `read_all_orders` | `read_orders`, `read_customers` and `read_all_orders` are installed; `read_returns`/`read_locations` are absent. Resolve Return access and verify exact field/protected-data requirements before canonical acceptance |
| POS filtering, explicit location/cutoff configuration and all-off defaults; parent verified the approved sole active retail location **83278725336 — 2266 Senter Road** | Runtime configuration for verified Shop GID **75779080408**/domains, launch cutoff, released app configuration and loyalty subscription provisioning, production worker throughput, rate/cost capacity and live monitoring; location POS UI shows dash/None, so Pro is not confirmed and no Pro requirement is established |
| Completed non-admin receipt evidence is minimized at configured age | Canonical/ledger/AuditLog/backup disposal or archival enforcement; configured durations alone do not enforce these policies |

Official API references and exact reader limitations are linked in [runbook.md](runbook.md). These establish schema contracts, not this shop's live state.

## Precise missing inputs and gates

| Missing input or approval | Required before |
|---|---|
| Configure the verified production Shop GID **75779080408** and consistent domain/signing bindings in a separately approved launch setup; parent has resolved the canonical/alias relationship, while runtime values remain unset | Receiving or canonical processing |
| Immutable UTC launch cutoff, intentionally unset until activation; sole retail Location GID **83278725336** is already approved and verified active | Canonical eligibility processing/posting |
| Named exception owner | Posting and operational review handoff |
| Receipt, canonical evidence, ledger and backup retention durations plus an approved archival/disposal procedure | Applicable receiving/processing/posting gates; non-expiring balances and dedupe/accounting integrity must survive evidence lifecycle decisions |
| Explicit spending budget and measured capacity/rate limits | Processing/activation approval |
| Resolve missing installed `read_returns`; verify minimum customer-ID/location field and protected-data access. `read_orders`, `read_customers` and `read_all_orders` are already installed; `read_locations` is absent | Authorized real integration/shadow verification and canonical processing/recovery |
| Review released app/TOML subscriptions and exact loyalty targets/signing ownership; API-created inventory is complete (2 existing, neither at loyalty endpoint). Separate provisioning approval remains required | Any subscription changes |
| Review of this uncommitted diff, exact deployment target/version, DB backup/restore rehearsal and explicit deployment approval | Deployment, even with flags OFF, because startup installs additive tables and permission defaults |
| Authorized real canonical/shadow validation, observability and recovery checks | Explicit receiving/processing/posting activation decisions |
| Reviewed successful-merge/identity-hold release procedure | Resolving those held identities; no release button is implemented |

All unspecified production activation values remain unset in application source; all five receiving/processing/posting/POS-read/all-staff flags default OFF. Verified DEV IDs are confined to the dedicated development setup and are not production launch inputs. Demo times and policy values remain synthetic. No credentials should be placed in this packet.

## Planned rollback and release sequencing — not authorization

1. Review this packet and diff locally; resolve attributable test failures. No commit is requested, and no commit may be made before required tests pass.
2. Separately approve an exact disabled deployment, target, additive schema/permission effects and database backup/restore procedure. Never replace newer ledger history with an old backup.
3. Inspect actual schema readiness after authorized deployment with all flags OFF. Verify unchanged inventory/TikTok paths and install/scope/subscription facts through separately authorized read access.
4. Supply the missing activation inputs, approve shadow processing and verify real canonical cases before posting. Keep receiving, processing and posting as independent decisions.
5. For an accounting defect, pause posting while retaining healthy intake/backlog where appropriate. For a worker defect, pause processing. For intake/security defects, pause receiving. These are planned operational actions, not actions performed here.
6. Roll back compatible application code without dropping loyalty tables or deleting balances/history. Expired worker leases are fenced. Incorrect postings require authorized compensating entries, never ledger UPDATE/DELETE. Surface paused/stale balances and retained exceptions.

Full canonical/ledger/backup retention disposal and successful-merge release remain explicit limitations. No point expiration, reward redemption, Shopify mutation, production restart or public-site work is part of this release.

## Phase two — local implementation delivered, access disabled

[phase-two-prd.md](phase-two-prd.md) and [phase-two-plan.md](phase-two-plan.md) now record approved local implementation. The dedicated verified-bearer read endpoint and two native customer-details targets reuse the existing ledger; no earning, corrections, bulk or contact queries were added to POS. Both POS read and all-staff policy flags default OFF, and real app/shop values remain unset.

The implemented option allows all staff using an explicitly approved app/shop authenticated session to view shop-scoped balances/history without individual Ops bindings or extra login. Jeffrey approved the recommendation for all cashiers to view balances/history on September 11, with corrections restricted to existing authorized Ops controls. This explicit policy approval does not enable either runtime flag or grant production access. PIN identity is not verified or an authorization boundary. If a future policy requires restricted roles, the PRD preserves per-user alternatives for further design; those alternatives are outside this implementation. Administrator corrections remain in Ops.

[phase-two-runbook.md](phase-two-runbook.md) retains chronological setup commands and earlier results. The current test matrix above supersedes its older counts and temporary-preview status. POS UI API is pinned to **2026-07** and shared Admin API remains **2026-04**. The existing synthetic component preview is **http://127.0.0.1:8767/** (Windows loopback); reload it for the updated review guidance. It is not native Shopify POS. Existing native preview/process/tunnel ownership stays with the parent; no temporary URL is asserted live by this review.

| Evidence boundary | What is established |
|---|---|
| Prior parent read-only UI and authorized DEV setup | Organization **Degen Collectibles / 169353178**; **Degen Loyalty Dev / 422232031233**, linked client **75c6c90117c95bd9f683cacaf3ed5c51**; **degen-loyalty-dev.myshopify.com**, Basic Development store, Shop **81339547671**; DEV location **104761688087**, active POS Pro; iPad/iPhone POS installed. The earlier initial app URL `example.com`, empty configured scopes and zero installs describe creation, not current preview installation. |
| Prior authorized DEV native preview + user screenshot | Native registration/token/backend path reached the synthetic ledger for **Loyalty Test / 9588918190103**: balance **19**, history **−1/+20**, compact block and header Refresh. This is seeded evidence, not a real Shopify purchase/refund accounting integration test. |
| Current automated verification | Exact-app/shop bearer denial tests, read-only SQL enforcement, all-staff reads without Ops binding, cashier correction denial, context isolation/offline/reconnect/error recovery and supported component layout. No verified PIN identity is claimed. |
| Historical production app UI evidence | Parent previously observed **Degen Ops / 346302742529**, active version **degen-ops-4 / 933925158913**. `read_returns` was absent from the displayed configured scopes; the subsequent installed-scope query above confirms it is also absent from actual grants. Protected-data access remains unverified. This app is not an approved development target or selected production loyalty app. The sole retail location has since been approved separately, below. |
| Parent live production location evidence / user approval | **2266 Senter Road — gid://shopify/Location/83278725336** is the sole active retail location, verified in Shopify admin and approved by Jeffrey for the launch plan. Location detail shows **Subscription None**; the POS subscription page shows **Plan —**. Pro is not confirmed; these displays do not establish a Pro requirement or authorize an upgrade. |
| Still unverified | Runtime/signing configuration for verified Shop **75779080408** and domains, production distribution/POS signing setup, resolution of missing `read_returns` and minimum field/protected-data access, POS capabilities/requirements and device versions, stable HTTPS/proxy configuration, live canonical accounting evidence, post-deploy readiness and backup integrity/restore. Host metadata is verified above. The sole retail location is verified/approved; its observed plan displays are dash/None, not Pro confirmation. Phone offline/reconnect/sleep/lock and native customer-switch lifecycle have no user confirmation; simulations cannot establish those facts. |

Official requirements rechecked September 11: Shopify's [Session API](https://shopify.dev/docs/api/pos-ui-extensions/latest/target-apis/standard-apis/session-api) issues tokens for the authenticated user, not the pinned staff member, and can return no token without app permission. [Server communication](https://shopify.dev/docs/apps/build/pos/communicate-with-server) requires HTTPS and both documented Shopify extension origins. [Customer-details targets](https://shopify.dev/docs/api/pos-ui-extensions/latest/targets/customer-details), [POS block](https://shopify.dev/docs/api/pos-ui-extensions/latest/web-components/layout-and-structure/pos-block) and [Page](https://shopify.dev/docs/api/pos-ui-extensions/latest/web-components/layout-and-structure/page) support the implemented customer context and single native action slots. [Native setup](https://shopify.dev/docs/apps/build/pos/getting-started) requires a development store and mobile POS app; the real Dev Console provides its mobile preview link. [POS app permissions](https://help.shopify.com/en/manual/your-account/users/roles/permissions/pos-permissions) are distinct from our approved cashier read policy. No plan purchase or permission change follows from this review.

The [Order query documentation](https://shopify.dev/docs/api/admin-graphql/2026-04/objects/QueryRoot) confirms the default 60-day order horizon; the production installation now verifiably has `read_all_orders` alongside `read_orders`. This does not authorize historical credit or live order-data reads. The reader's `read_returns` prerequisite and exact 2026-04 field validation remain the earlier source-reviewed evidence in [runbook.md](runbook.md); the earlier review could not refetch the version-specific Return page. The [current Return reference](https://shopify.dev/docs/api/admin-graphql/latest/objects/Return) does not establish installed production grants or justify changing our pinned API. No Admin scopes are needed merely to read this local ledger in POS; canonical earning integration requires the separately verified reader access.

Next release decision is about the concrete missing production inputs and integration/deployment gates above, not cashier-policy reapproval. Before any later activation, verify stable HTTPS and exact app/shop binding, production schema/read access, named exception handling, capacity across worker processes (the POS limiter is process-local), backup/retention procedures and authorized canonical/shadow cases. Retention disposal and successful-merge release remain explicit limitations. If an authorized native acceptance session is needed, consolidate the unverified lifecycle cases; do not repeat the already confirmed 19-point/history visual walkthrough. Planned POS rollback is to disable POS reads/all-staff runtime flags and remove the extension presentation through a separately authorized release, preserving the ledger and independent accounting gates.
